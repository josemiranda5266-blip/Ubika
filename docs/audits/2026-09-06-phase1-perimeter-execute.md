# Phase 1 perimeter execution

This marker intentionally triggers the one-time perimeter hardening workflow. It performs only perimeter code changes; tests, lint, typecheck and build remain deferred until the end of the roadmap.
